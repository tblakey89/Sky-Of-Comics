<% provide(:title, "Users") %>
<h1>All Users</h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= image_tag user.avatar_url(:thumb) unless user.avatar.blank? %>
      <%= link_to user.username, user %>
    </li>
  <% end %>
</ul>
