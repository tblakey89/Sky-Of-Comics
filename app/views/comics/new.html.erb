<% provide(:title, "New Comic") %>

<h1>New Comic</h1>
<div>
  <%= form_for(@comic) do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :description %>
    <%= f.text_area :description %>

    <%= f.submit "Create" %>
  <% end %>
</div>
